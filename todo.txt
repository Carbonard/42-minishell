built-ins
	- cd
	- errores

free at exit
	- input
	- tree
	- si falla env

check unset VAR=value
(echo hola) > file

echo $CMD | ./minishell
'echo hola
echo adios'

señales
	- ctrl + D mientras escribir input antes y despues de un salto de linea

echo hola > /dev/stdout | sleep 3 | echo adios

echo hola || echo adios && echo si

errores:
	- en execute_leaf si algo falla
	- en builtins traducir errores

señales
	- ctrl + C
	- ctrl + \

Nombre del binario en errores

dos redirecciones deberian crear archivos

redirecciones en builtins

leer hasta metacaracteres

variables staticas 

echo ${REAL_VAR}aaaa

exit exit

cat << EOF | grep -e |>>''

cat
^C

*

cosas sin permisos

mensaje error de command not found

export ?=algo

redirecciones builtins

export a=' hola'
echo$a		-> imprime hola
echo"$a"	-> echo hola command not found

exit status cuando commando sin permisos

cd too many arguments

ls fadfa || (echo $? && echo $?)

expand ~

just one heredoc (?)

echo |
^C

echo |
^D

echo |||